# FastAPI & Server
fastapi
uvicorn[standard]
pydantic>=2.11.3  # Required by a2a-sdk
pydantic-settings
python-dotenv
python-multipart
aiofiles

# Logging & Telemetry
structlog

# Microsoft Agent Framework (using beta version as stable not yet released)
# This will automatically install compatible versions of:
# - opentelemetry packages
# - azure-monitor-opentelemetry packages  
# - websockets (via mcp[ws])
# - and other dependencies
agent-framework

# Azure dependencies (app-specific, not covered by framework)
azure-identity
azure-ai-documentintelligence  # Document Intelligence SDK
azure-cognitiveservices-speech  # Speech Services
azure-storage-blob
azure-cosmos

# YAML support
pyyaml>=6.0.1

# AI/ML
openai>=1.99.0  # Updated to match agent-framework requirement

# Data Processing
pandas==2.2.3
numpy>=2.2.6  # Updated to match agent-framework-redis requirement

# File Processing
PyPDF2==3.0.1
pypdf==5.0.1
python-magic==0.4.27; sys_platform == 'linux' or sys_platform == 'darwin'  # File type detection (Linux/Mac)
python-magic-bin==0.4.14; sys_platform == 'win32'  # File type detection (Windows)
pillow==10.4.0  # Image processing
# moviepy removed - will use Azure Video Indexer for video processing
pydub==0.25.1  # Audio format conversion

# PDF Export
reportlab==4.2.5
markdown==3.7
pdfkit==1.0.0  # HTML to PDF (optional, has fallback to ReportLab)

# HTTP & Requests
requests==2.32.3
httpx>=0.28.1  # Updated to match a2a-sdk requirement

# Utilities
tabulate==0.9.0
python-dateutil==2.9.0

# Development
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
black==24.10.0
ruff==0.7.3
