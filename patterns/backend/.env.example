# Azure OpenAI Configuration (Required for Agent Framework)
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2024-10-21

BING_CONNECTION_ID=
AZURE_AI_PROJECT_ENDPOINT=https://<foundry>.services.ai.azure.com/api/projects/<project>
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o-mini

# Optional: Azure tenant ID (if not using default tenant)
# AZURE_TENANT_ID=your-tenant-id
MODEL_PROVIDER=github

# Optional: API Version for Azure OpenAI
AZURE_OPENAI_API_VERSION=2024-10-21

# ============================================================================
# Deep Research Pattern Configuration (Optional)
# ============================================================================

# Optional: Application Insights for tracing (visible in Foundry > Tracing)
# APPLICATIONINSIGHTS_CONNECTION_STRING=<Your App Insights connection string>
# AZURE_TRACING_GEN_AI_CONTENT_RECORDING_ENABLED=true

# Optional: Role-based access control (viewer | doc-reader | analyst | admin)
# USER_ROLE=viewer

# Optional: Vector store for private search mode
# AZURE_VECTOR_STORE_ID=vs_123

# Optional: PDF path for multimodal research mode
# PDF_PATH=C:\path\to\research_document.pdf